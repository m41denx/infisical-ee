name: Release standalone docker image
on:
    push:

jobs:
    infisical-standalone:
        name: Build infisical standalone image postgres
        runs-on: ubuntu-latest
        # needs: [infisical-tests]
        steps:
            - name: Extract version from tag
              id: extract_version
              run: echo "::set-output name=version::${GITHUB_REF_NAME}"
            - name: ☁️ Checkout source
              uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 Nov 14, 2025
              with:
                  fetch-depth: 0
            - name: 📦 Install dependencies to test all dependencies
              run: npm ci --only-production
              working-directory: backend
            - name: version output
              run: |
                  echo "Output Value: ${{ steps.version.outputs.major }}"
                  echo "Output Value: ${{ steps.version.outputs.minor }}"
                  echo "Output Value: ${{ steps.version.outputs.patch }}"
                  echo "Output Value: ${{ steps.version.outputs.version }}"
                  echo "Output Value: ${{ steps.version.outputs.version_type }}"
                  echo "Output Value: ${{ steps.version.outputs.increment }}"
            - name: Save commit hashes for tag
              id: commit
              run: echo "short=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
            - name: 🔧 Set up Docker Buildx
              uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0
            - name: 🐋 Login to Docker Hub
              uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}
            - name: 📦 Build backend and push to Docker Hub
              uses: docker/build-push-action@v7 # Main / v1, Mar 10, 2026
              with:
                  push: true
                  context: .
                  tags: |
                      m41den/miculoid:latest
                      m41den/miculoid:${{ steps.commit.outputs.short }}
                      m41den/miculoid:${{ steps.extract_version.outputs.version }}
                  platforms: linux/amd64
                  file: Dockerfile.standalone-infisical
                  build-args: |
                      POSTHOG_API_KEY=""
                      INFISICAL_PLATFORM_VERSION=${{ steps.extract_version.outputs.version }}
                      DD_GIT_REPOSITORY_URL=${{ github.server_url }}/${{ github.repository }}
                      DD_GIT_COMMIT_SHA=${{ github.sha }}
                  secrets: |
                      contentful_space_id=${{ secrets.CONTENTFUL_SPACE_ID }}
                      contentful_delivery_token=${{ secrets.CONTENTFUL_DELIVERY_TOKEN }}
                      contentful_environment=${{ vars.CONTENTFUL_ENVIRONMENT }}
